{
  "builds": [
    {
      "src": "vercel_app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "vercel_app.py"
    }
  ],
  "installCommand": "uv pip install -r requirements.txt --system --no-cache-dir || uv sync --frozen"
}
